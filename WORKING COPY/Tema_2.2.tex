\section*{ТЕМА 2. СХОДИМОСТЬ И АППРОКСИМАЦИЯ В НОРМИРОВАННОМ ПРОСТРАНСТВЕ}
\addcontentsline{toc}{section}{{Тема 2. Сходимость и аппроксимация в
нормированном пространстве}}\label{sec_2.2}

\setcounter{theorem}{0} \setcounter{example}{0} \setcounter{exes}{0}
\setcounter{corollary}{0} \setcounter{definition}{0}
\setcounter{lemma}{0}


\textbf{Сходимость в нормированном пространстве}. Рассмотрим в
нормированном пространстве $E$ последовательность элементов
$(x_n)_{n=1}^\infty$.

  Последовательность $(x_n)\ss E,$  называется \textit{сходящейся}, если
  существует такой элемент $a\in E$, что
  $\|x_n-a\|\to 0$ при $n\to\infty,$
  т. е. для любого $\varepsilon>0 $ существует такое $
  n(\varepsilon),$  что для $n\geq n(\varepsilon)$ $$\|x_n-a\|<\varepsilon.$$ Если $a$ есть
  предел последовательности $(x_n)$, то будем писать $a=$
  $=\lim\limits_{n\to\infty} x_n$ или
  $x_n\to a$ при $n\to\infty.$


Назовем \textit{окрестностью} $O_\varepsilon(a)$ точки $a\in E$
любой открытый шар с центром в этой точке радиуса $\varepsilon$.
%
Тогда  можно  по-другому сформулировать определение сходящейся
последовательности: последовательность $(x_n)_{n=1}^\infty$ сходится
к $a$, если каждая окрестность $O_\varepsilon(a)$ точки $a$ содержит
все элементы $x_n$ последовательности $(x_n)_{n=1}^\infty$, начиная
с некоторого номера $n(\varepsilon).$

\vspace{5mm}\centerline{\textit{Свойства сходящихся
последовательностей}}\vspace{5mm}
%
1. В нормированном пространстве
  сходящаяся последовательность имеет только один предел.

2. Если последовательность $x_n$ сходится к
  $a$ в $E$, то любая ее подпоследовательность сходится также к
  $a$.

3. Сходящаяся в нормированном пространстве
  последовательность ограничена.


4. Если $x_n\to a,\,\lambda_n\to\lambda$ при
  $n\to\infty$, где $(\lambda_n)$ -- числовая последовательность, то
  $\lambda_n x_n\to\lambda x$ при $n\to\infty$.

5. Пусть $x_n\to a, y_n\to b$ при
  $n\to\infty$ в пространстве $E$, тогда $x_n+y_n\to a+b$.

6. Если последовательность $(x_n)$ сходится в $E$ к элементу $a$,
   то  числовая последовательность $(\|x_n\|)$ сходится в $\R$ к
  $\|a\|.$


\textbf{Аппроксимация в нормированном пространстве}. Рассмотрим
подпространство $L$ пространства $ E$ и определим для $x\in E$
расстояние до $L$ по формуле
$$
  \rho(x,L)=\inf\limits_{\ell\in L} \|x-\ell\|.
$$

Число $\rho(x,L)$ характеризует наилучшее приближение (наилучшую
аппроксимацию) элемента $x$  элементами подпространства $L.$

  Если существует элемент $y\in L$
  такой, что $\rho(x,L)=\|x-y\|$, то $y$ называется \textit{наилучшим
  элементом  приближения} $x$ элементами подпространства $L$ или
  \textit{элементом  наилучшей аппроксимации.}

\begin{theorem}[(о существовании аппроксимации)]\label{teo_2.7.1}
 Пусть $L$ -- конечномерное подпространство нормированного
 пространства $E$.  Для любого  элемента $x\in E$ существует
(возможно,   не  единственный) элемент $y\in L$, что
     $$
           \rho(x, L)=\|x-y\|.
     $$
\end{theorem}

 Нормированное векторное пространство $E$
 будем называть \textit{строго нормированным}, если в нем равенство
 $\|x+y\|= \|x\|+\|y\|$ возможно только при $y=\lambda x$, где
 $\lambda>0$.
%
\begin{theorem}[(о единственности аппроксимации)]\label{teo_2.7.2}
  В строго нормированном пространстве $E$ для каждого
  $x\in E$ и каждого подпространства $L\ss E$ может существовать не более одного
  элемента наилучшей аппроксимации $x$ элементами $L.$
\end{theorem}

\centerline{\textbf{ПРИМЕРЫ РЕШЕНИЯ ЗАДАЧ}}\vspace{5mm}

\textbf{Задача 1.} a) 


\newpage
